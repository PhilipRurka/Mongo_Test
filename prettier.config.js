const config = {
  formatOnSave: true,
  tabWidth: 2,
  printWidth: 120,
  singleQuote: true,
  trailingComma: 'es5',
  tailwindFunctions: ['clsx'],
  plugins: ['prettier-plugin-tailwindcss'],
}

module.exports = config